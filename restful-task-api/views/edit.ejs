<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit the Task</title>
</head>
<body>
    <fieldset>
        <legend>Edit a Task</legend>
        <form action="/tasks/<%= task._id %>" method='POST'>
        <div>
            <label for="">Title:</label><input type="text" name='title' value='<%= task.title %>' >
        </div>
        <div>
            <label for="">Description:</label><input type="text" name='description' value='<%= task.description %>'>
        </div>
        <div>
            <label for="">Task completed at:</label>
            <select name="complete" id="complete" value='<%= task.complete %>'>
                <option value="true">true</option>
                <option value="false">false</option>
            </select>
        </div>
        <div>
            <label for=""> Task created at:</label><input type="date" name='created_at' value='<%= task.created_at %>' >
        </div>
        <div>
            <label for="">Task updated at:</label><input type="date" name='updated_at' value='<%= task.updated_at %>'>
        </div>
        <input type="submit" value="submit">
    </form>
    </fieldset>
</body>
</html>